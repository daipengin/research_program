# Legacy programs archive

This directory contains the previous `research_program` source tree copied before
starting the graph-first Web GUI rebuild.

The active entry points in `src/research_program` are being replaced with the new
structure. The legacy application is kept here for reference only and is not
expected to remain runnable.

